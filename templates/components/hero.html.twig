{% block hero %}
    <main role="main" class="container">
        <div class="row">
            <h2 class="blog-post-title mt-5 mb-5 text-center" style="font-size: 45px;">
                {{ 'welcome_message'|trans({}, 'hero') }}
            </h2>
            <div class="col-md-8 blog-main mt-4">
                <div class="blog-post">
                    {% include '/components/carousel.html.twig'  with {recipes : recipes} only %}
                    <p class="blog-post-meta"></p>
                    <p>{{ 'paragraph_1'|trans({}, 'hero') }}</p>
                    <hr>
                    <p>{{ 'paragraph_2'|trans({}, 'hero') }}</p>
                    <p>{{ 'paragraph_3'|trans({}, 'hero') }}</p>
                    <p>{{ 'paragraph_4'|trans({}, 'hero') }}</p>
                </div>
                <div class="mt-5">
                    <h4 class="font-italic">{{ 'title_1'|trans({}, 'hero') }}</h4>
                    <ol class="list-unstyled">
                        <li><a href="https://github.com/AdrienHq">{{ 'link_1'|trans({}, 'hero') }}</a></li>
                        <li><a href="https://www.instagram.com/adrienhecq/?hl=fr">{{ 'link_2'|trans({}, 'hero') }}</a></li>
                    </ol>
                </div>
            </div>

            <aside class="col-md-4 blog-sidebar mt-4 p-3">
                <h3 class="font-weight-bold mb-4">{{ 'title_2'|trans({}, 'hero') }}</h3>
                {% for recipe in popularRecipes %}
                    <div class="p-3 mb-3 recipe-card">
                        <div class="row no-gutters">
                            <div class="col-8">
                                <h5 class="font-italic">{{ recipe.name }}</h5>
                                <p class="text-muted views-text">Views: {{ recipe.numberViews }}</p>
                                {% for category in recipe.category %}
                                    <span class="badge badge-info mr-1">{{ category.name | default('') }}</span>
                                {% endfor %}
                            </div>
                            <div class="col-4 d-flex align-items-center">
                                <a href="{{ path('recipe.detail', {id: recipe.id, slug: recipe.slug } ) }}" class="d-block w-100">
                                    {% if recipe.thumbnail is not empty %}
                                        <img src="{{ vich_uploader_asset(recipe, 'thumbnailFile') }}" class="img-fluid rounded recipe-image" alt="{{ recipe.name }}">
                                    {% else %}
                                        <img src="{{ asset('defaultImage.jpg') }}" alt="image#{{ recipe.id }}" class="img-fluid rounded recipe-image">
                                    {% endif %}
                                </a>
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </aside>
        </div>
    </main>
{% endblock %}