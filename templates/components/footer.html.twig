{% block footer %}
    <footer class="d-flex flex-wrap justify-content-between align-items-center py-3 my-4 border-top">
        <ul class="nav col-md-8 justify-content-start">
            <li class="nav-item">
                <a href="{{ path('legals') }}" class="nav-link px-2 text-muted">{{ 'terms'|trans({}, 'footer') }}</a>
            </li>
            <li class="nav-item">
                <a href="{{ path('privacy_policy') }}" class="nav-link px-2 text-muted">{{ 'privacy'|trans({}, 'footer') }}</a>
            </li>
            <li class="nav-item">
                <a href="{{ path('cookie_policy') }}" class="nav-link px-2 text-muted">{{ 'cookies'|trans({}, 'footer') }}</a>
            </li>
            <li class="nav-item">
                <a href="{{ path('about') }}" class="nav-link px-2 text-muted">{{ 'about'|trans({}, 'footer') }}</a>
            </li>
            <li class="nav-item">
                <a href="{{ path('contact') }}" class="nav-link px-2 text-muted">{{ 'contact'|trans({}, 'footer') }}</a>
            </li>
        </ul>
        <ul class="nav col-md-4 justify-content-end">
            <span class="nav-link px-2 text-muted">{{ 'copyrights'|trans({}, 'footer') }}</span>
        </ul>
    </footer>
{% endblock %}
