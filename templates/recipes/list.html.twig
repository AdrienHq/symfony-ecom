{% extends "base.html.twig" %}

{% block body %}

    <div class="container">
        <div class="row">
            <div class="col-md-3">
                {% include '/recipes/_filter.html.twig' with {form: form } only %}
            </div>
            <div class="col-md-9">
                <div class="grid">
                    {% for recipe in recipes %}
                        {% include '/recipes/recipeCard.html.twig' with {recipe: recipe } only %}
                    {% endfor %}
                </div>
{#                <div class="pagination">#}
{#                    {{ knp_pagination_render(recipes) }}#}
{#                </div>#}
            </div>
        </div>
    </div>
{% endblock %}

