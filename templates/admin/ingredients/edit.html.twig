{% extends "/admin/admin.html.twig" %}

{% block title "Edit ingredient : " ~ ingredient.name %}

{% block body %}
    <h1>{{ ingredient.name |capitalize }}</h1>
    {{ form_start(form) }}
    {{ form_end(form) }}
{% endblock %}
