{{ form_start(form, {attr: {class: 'filter'}}) }}
{{ form_row(form.q) }}

<h4>{{ 'filter.ingredients'|trans({}, 'fillerWords') }}</h4>
{{ form_row(form.category) }}
<h4>{{ 'filter.prep_time'|trans({}, 'fillerWords') }}</h4>
<div class="row">
    <div class="col-md-6">
        {{ form_row(form.minDuration) }}
    </div>
    <div class="col-md-6">
        {{ form_row(form.maxDuration) }}
    </div>
</div>

<h4>{{ 'filter.vege'|trans({}, 'fillerWords') }}</h4>
{{ form_row(form.vegetarian) }}

<button type="submit" class="btn btn-success w-100">{{ 'filter.filter'|trans({}, 'fillerWords') }}</button>

{{ form_end(form) }}