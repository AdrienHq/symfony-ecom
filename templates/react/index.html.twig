{# templates/home.html.twig #}
{% extends 'base.html.twig' %}

{% block body %}
    <div {{ react_component('Home') }}></div>
    <h3 class="text-violet-700">2 eme react element</h3>
    <recipes-list data-post="{{ 1 }}" data-user="¨{{ app.user ? app.user.id :0 }}"></recipes-list>
{% endblock %}